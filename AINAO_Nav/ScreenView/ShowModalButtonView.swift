//
//  ShowModalButtonView.swift
//  DraftSheet
//
//  Created by Vincent Van hoylandt on 27/06/2024.
//

import SwiftUI
/*
struct ShowModalButtonView: View {
    
    @State private var showModal: Bool = false
    
    var body: some View {
        Button(action: {
                self.showModal = true
        }) {
            Capsule()
                    .fill(Color.red)
                    .opacity(0.8)
                    .frame(width: 100, height: 5)
                    .padding(6)
        }
        // fullScreenCover
        // formsheet
        // sheet
                .sheet(isPresented: $showModal) {
                    ModalView()
                        .presentationDetents([.fraction(0.25), .fraction(0.70), .large])
                        .presentationDragIndicator(.visible)
                }
    }
}

#Preview {
    ShowModalButtonView()
}
*/
